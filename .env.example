### ===========================================
### Required Configuration
### ===========================================

### Google Cloud Vertex AI Model Authentication
GOOGLE_GENAI_USE_VERTEXAI=TRUE
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1

### Agent name to identify cloud resources and logs
AGENT_NAME=agent-foundation

### OpenTelemetry message content capture (true/false) - https://opentelemetry.io/blog/2024/otel-generative-ai/#example-usage
OTEL_INSTRUMENTATION_GENAI_CAPTURE_MESSAGE_CONTENT=TRUE


### ===========================================
### GitHub CI/CD Configuration
### ===========================================
GITHUB_REPO_NAME=agent-foundation
GITHUB_REPO_OWNER=your-github-username-or-org


### ===========================================
### Optional Agent App Runtime Configuration
### ===========================================

### Logging verbosity (default: INFO)
# LOG_LEVEL=DEBUG

### OpenTelemetry service namespace to group traces and logs (default: local)
# TELEMETRY_NAMESPACE=local-developer-unique-name

### Server configuration override (defaults: HOST=127.0.0.1, PORT=8000)
# HOST=127.0.0.1
# PORT=8000

### Enable ADK web UI at http://127.0.0.1:8000 (default: false)
# SERVE_WEB_INTERFACE=TRUE

### Enable the ADK web app to reload agents on file changes (default: false)
# RELOAD_AGENTS=TRUE

### Session and Memory persistence via Agent Engine (default: in-memory ephemeral service)
### NOTE: Agent Engine instance must be created externally
# AGENT_ENGINE=projects/123/locations/us-central1/reasoningEngines/456

### Artifact storage via GCS bucket (default: in-memory ephemeral service)
### NOTE: GCS bucket must be created externally
# ARTIFACT_SERVICE_URI=gs://your-artifact-storage-bucket

### CORS allowed origins (JSON array string, default: ["http://127.0.0.1", "http://127.0.0.1:8000"])
# ALLOW_ORIGINS='["https://your-domain.com", "http://127.0.0.1:3000"]'

### Override default root agent model (default: gemini-2.5-flash)
# ROOT_AGENT_MODEL=gemini-2.5-flash

### Other optional features
# ADK_SUPPRESS_EXPERIMENTAL_FEATURE_WARNINGS=TRUE
